@page
@using Net.Leksi.Pocota.Tool.Pages.CS
@using Net.Leksi.Pocota.Tool
@using Net.Leksi.Pocota.Server
@model IPocotaEntityModel
@{
}
@Html.Partial("Header", Model)
public interface @Model.ClassName@(Model.Inheritances.Count > 0 ? Html.Raw($": {string.Join(", ", Model.Inheritances)}") : string.Empty)
{
@foreach(PropertyModel pm in Model.Properties)
{
    <text>    @nameof(EntityProperty) @pm.Name { get; }
</text>
}
}